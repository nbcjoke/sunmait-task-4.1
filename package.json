{
  "scripts": {
    "postinstall": "(cd client && npm install) && (cd ../server && npm install)",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.3",
    "pretty-quick": "^3.1.3"
  }
}
